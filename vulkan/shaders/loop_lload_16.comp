#version 450

layout(local_size_x = 32) in;

shared float sm[16];

layout(std430, binding = 0) buffer Out { float out_data[]; };

void main() {
    float acc = 0.0;
    for (int i = 0; i < 16; i++) {
        acc += sm[i];
    }
    out_data[0] = acc;
}
